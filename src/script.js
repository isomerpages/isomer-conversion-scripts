// What this script will do (in order)
  // 1. create a new staging branch, staging-next-gen
  // 2. add files
  // 3. modify files
  // 4. remove files
// to make the repo compatible with V2 templates

// import dependencies
const conversionTools = require('./localUtils')
